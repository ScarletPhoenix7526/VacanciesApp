<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.coder.laboratory2_vacancies">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.CALL_PHONE"/>

    <application
        android:name=".StartApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_vacancy"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_vacancy"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".main.MainPageActivity" />
        <activity android:name=".details.DetailsPageActivity" android:label="@string/details_page_label"/>
        <activity android:name=".favorites.FavoriteActivity" android:label="@string/text_favorite_on_toolbar"/>
        <activity android:name=".search.SearchingPageActivity" android:label="@string/searching"/>
    </application>
</manifest>